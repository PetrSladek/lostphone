<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:orientation="vertical"
    android:gravity="center"
    tools:context=".LockScreenActivity">


    <ImageView
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:id="@+id/image"
        android:src="@drawable/lock"
        android:scaleType="centerInside"
        />

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/btnOwnerCallText"
        android:id="@+id/btnCallOwner"
        android:onClick="onClickBtnCallOwner" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="V případě nálezu telefonu mi prosím zavolejte zpátky"
        android:textSize="24sp"
        android:textAlignment="center"
        android:padding="10dp"
        android:background="#000000"
        android:textColor="@color/wallet_bright_foreground_holo_dark"
        android:id="@+id/displayText"
         />



    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="numberPassword"
        android:gravity="center"
        android:ems="10"
        android:id="@+id/editUnlock"
        android:hint="@string/editUnlockHint"
        android:imeOptions="actionDone"
        />
    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/btnUnlockText"
        android:id="@+id/btnUnlock"
        android:onClick="onClickBtnUnlock"
        />

</LinearLayout>
